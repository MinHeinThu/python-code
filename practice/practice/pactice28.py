# Binary Search Tree has parent, left_child and right_child
# parent is lest than right_child
# parent is greater than left_child
# Balanced, perfect, full, complete are four types BST
# Traversal
"""
DFS recursively, stack(last in first out)
pre_orderK(node->left->right)
in_order(left->node->right)
post_order(left->right->node)

BFS level order traversal : queue (first in first our)
level by level 
"""
